import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    marginHorizontal: 30
  },
  image: {
    height: 300,
    marginBottom: 20,
    width: 350
  }
});

export default styles;
